const plus = (a, b) => a + b;
const timesTwo = (a, b) => a * 2;
const dividedByTwo = (a, b) => a / b;
const toThePowerOfTen = (a) => Math.pow(a, 10);

module.exports = {
  plus,
  timesTwo,
  dividedByTwo,
  toThePowerOfTen,
};
